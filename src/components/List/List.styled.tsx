import styled from 'styled-components'

export const ListStyled = styled.ul`
    list-style:none;
    padding:0;
    margin: 0;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: space-between;

    li {
        padding:0;
        margin:0;
    }

`